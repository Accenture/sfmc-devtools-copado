{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Apex Replay Debugger",
      "type": "apex",
      "request": "launch",
      "sfdxProject": "${workspaceRoot}"
    },
    {
      "type": "node-terminal",
      "name": "debug: cop_retrieve",
      "request": "launch",
      "command": "npm run cop_retrieve",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/copado-function/environments/Retrieve.env",
    }
  ]
}